name: sf_urban_health
version: 1.0.0
config-version: 2

profile: sf_urban_health

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  sf_urban_health:
    smoke:
      +tags: [smoke]
      +materialized: table
      +file_format: iceberg
