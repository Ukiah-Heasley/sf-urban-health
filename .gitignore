.env
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Backfill staging directory at repo root only; do NOT match dashboard/data/.
/data/
airflow/logs/
airflow/plugins/

# dbt project (top-level, canonical)
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Mirrored copy of dbt/ baked into the Airflow image (regenerated by `make sync-dbt`)
airflow/include/dbt/
airflow/include/contracts/

# Airflow runtime artifacts
airflow_settings.yaml
airflow-webserver.pid
webserver_config.py
airflow.cfg
airflow.db
astro

.DS_Store
.idea/
.vscode/
.claude/

# Private working notes. Public documentation must describe implemented behavior only.
/ROADMAP.md
/learning/

# Local scratch/archive area (never commit).
/tmp/

# Local lakehouse Docker stack (MinIO data, optional overrides)
lakehouse/.env
lakehouse/.env.local
lakehouse/.env.aws

# Active and mode-specific Airflow env files
airflow/.env
airflow/.env.local
airflow/.env.aws
